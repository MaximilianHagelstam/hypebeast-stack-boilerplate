{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky install",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "husky": "^7.0.0"
  }
}
